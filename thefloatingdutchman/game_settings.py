# Basic Globals
BLACK = (0, 0, 0)
GRAY = (100, 100, 100)
WHITE = (255, 255, 255)
RED = (255, 0, 0)
GREEN = (0, 255, 0)
BLUE = (0, 0, 255)
YELLOW = (255, 255, 0)

# Game Settings
WINDOW_WIDTH = 1920
WINDOW_HEIGHT = 1080
FPS = 60

# Menu Button Bounds
WIDTH_LEFT_BOUND = WINDOW_WIDTH * (3 / 8)
WIDTH_RIGHT_BOUND = WINDOW_WIDTH * (5 / 8)

# Game Over Menu
PLAY_AGAIN_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (9 / 20)
PLAY_AGAIN_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * (11 / 20)
QUIT_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (3 / 5)
QUIT_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * (7 / 10)
GAME_OVER_MENU_Y_LOCATIONS = [WINDOW_HEIGHT*.2, WINDOW_HEIGHT*.45, WINDOW_HEIGHT*.6]

#Pause Menu
RESUME__HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (35 / 100)
RESUME_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((35 / 100) + (1 / 14))
OPTIONS_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (44 / 100)
OPTIONS_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((44 / 100) + (1 / 14))
VIEW_MAP_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (53 / 100)
VIEW_MAP_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((53 / 100) + (1 / 14))
VIEW_CONTROLS_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (62 / 100)
VIEW_CONTROLS_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((62 / 100) + (1 / 14))
RESTART_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (71 / 100)
RESTART_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((71 / 100) + (1 / 14))
QUIT_PAUSE_HEIGHT_LOWER_BOUND = WINDOW_HEIGHT * (80 / 100)
QUIT_PAUSE_HEIGHT_UPPER_BOUND = WINDOW_HEIGHT * ((80 / 100) + (1 / 14))
